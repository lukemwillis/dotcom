import React from "react";
import Link from "gatsby-link";

const Logo = () => (
  <Link to="/">
    //lukemwillis.com/
    {/* <h1 style={{
      padding: 0,
      margin: 0,
      position: 'fixed',
      top: 0,
      left: '1em',
      transform: 'rotate(90deg)',
      transformOrigin: '0 0',
      fontSize: '13vh',
      whiteSpace: 'nowrap',
      textShadow: 'white 0.5vh 0.5vh, white 0.5vh -0.5vh, white -0.5vh 0.5vh, white -0.5vh -0.5vh'
    }}>
      <span style={{
        position: 'absolute',
        zIndex: 'auto'
      }}>L</span><span style={{
        display: 'none'
      }}>uke </span>
      <span style={{
        position: 'absolute',
        zIndex: -1,
        left: '4.4vh'
      }}>M</span><span style={{
        display: 'none'
      }}>. </span>
      <span style={{
        position: 'absolute',
        zIndex: -2,
        left: '10.2vh'
      }}>W</span><span style={{
        display: 'none'
      }}>illis</span>
    </h1> */}
  </Link>
);

export default Logo;
